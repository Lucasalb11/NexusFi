# ===========================================
# NexusFi - Variáveis de ambiente (exemplo)
# Copie para .env e preencha. NUNCA commite .env
# ===========================================

# --- Backend ---
NODE_ENV=development
PORT=3001
API_SECRET_KEY=

# --- Frontend (NEXT_PUBLIC_ = exposto no client) ---
NEXT_PUBLIC_API_URL=http://localhost:3001

# --- Soroban / Stellar ---
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
SOROBAN_NETWORK_PASSPHRASE=Test SDF Network ; September 2015
# Chave secreta apenas em backend/env, nunca no frontend
SOROBAN_SECRET_KEY=

# --- Chainlink / CRE (quando aplicável) ---
CHAINLINK_OPERATOR_KEY=
CHAINLINK_OPERATOR_SECRET=

# --- Produção (usar em deploy) ---
# NODE_ENV=production
# NEXT_PUBLIC_API_URL=https://api.nexusfi.example.com
