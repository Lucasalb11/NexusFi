[workspace]
resolver = "2"
members = ["nexusfi_token", "credit_score", "credit_line"]

[workspace.dependencies]
soroban-sdk = "22.0.10"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true
