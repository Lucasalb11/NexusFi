# ==============================================================================
# NexusFi CRE Workflows — Environment Variables
# ==============================================================================
# Used by CRE CLI during simulation and deployment.
# NEVER commit .env or secrets.yaml to git.
# ==============================================================================

# Ethereum private key for EVM writes (Sepolia testnet)
# CRE_ETH_PRIVATE_KEY=0x...
